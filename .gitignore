stack.yaml.lock
.stack-work
.pre-commit-config.yaml
result*
*.dump
*.tix
dist-newstyle
*.html
# Golden HTML output is source, not a build artifact; the rule above is aimed
# at haddock and coverage output.
!**/test_resources/*.html
