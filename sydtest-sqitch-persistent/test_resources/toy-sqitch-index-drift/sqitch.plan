%syntax-version=1.0.0
%project=toy-index-drift
%uri=sydtest-sqitch/toy-index-drift

init 2026-01-01T00:00:00Z syd <syd@example.com> # Create widget table with an index the persistent model does not have
